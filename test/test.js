(function () {
	describe('Mixpanel test suite', function () {

		describe('General API', function () {
			it('should be defined', function () {
				expect(window.WisemblyMixpanel).to.be.an('object');
			});
		});

	});
}());
